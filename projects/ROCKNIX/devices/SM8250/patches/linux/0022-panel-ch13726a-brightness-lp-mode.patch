diff -rupbN linux.orig/drivers/gpu/drm/panel/panel-ddic-ch13726a.c linux/drivers/gpu/drm/panel/panel-ddic-ch13726a.c
--- linux.orig/drivers/gpu/drm/panel/panel-ddic-ch13726a.c	2026-04-01 00:00:00.000000000 +0000
+++ linux/drivers/gpu/drm/panel/panel-ddic-ch13726a.c	2026-04-01 00:00:01.000000000 +0000
@@ -245,17 +245,12 @@ static int ch13726a_bl_update_status(str
 {
 	struct mipi_dsi_device *dsi = bl_get_data(bl);
 	u16 brightness = backlight_get_brightness(bl);
-	int ret;
 
-	dsi->mode_flags &= ~MIPI_DSI_MODE_LPM;
-
-	ret = mipi_dsi_dcs_set_display_brightness(dsi, brightness);
-	if (ret < 0)
-		return ret;
-
-	dsi->mode_flags |= MIPI_DSI_MODE_LPM;
-
-	return 0;
+	/* Send brightness DCS command in LP mode (default).
+	 * HS mode causes FIFO corruption + timeout (dsi_err_worker status=5)
+	 * when the command collides with the video data stream during BLLP.
+	 * LP mode uses the escape clock lane and is safe during video. */
+	return mipi_dsi_dcs_set_display_brightness(dsi, brightness);
 }
 
 static const struct backlight_ops ch13726a_bl_ops = {
