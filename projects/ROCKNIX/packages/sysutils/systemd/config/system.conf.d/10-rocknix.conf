# ROCKNIX: reduce service stop timeout and enable shutdown watchdog.
# The default 90s stop timeout causes shutdown/reboot to hang when
# bash-loop services (powerstate, fancontrol) don't respond to SIGTERM.
[Manager]
DefaultTimeoutStopSec=10s
ShutdownWatchdogSec=30s
